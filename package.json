{
  "name": "@f/set-attribute",
  "description": "Set an attribute on a DOM element",
  "repository": "git://github.com/micro-js/set-attribute.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/svg-attribute-namespace": "^2.1.1"
  },
  "devDependencies": {
    "hihat": "^2.5.0",
    "tape": "^4.2.2"
  }
}
